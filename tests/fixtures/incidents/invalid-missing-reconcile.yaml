id: INC-K8S-03
chapter: 14
tier: Core-must
scope: {environment: dev, namespaces: [app-dev], forbiddenNamespaces: [app-prod]}
maximumDuration: 5m
inject: {manifest: charts/mini-commerce/templates/networkpolicy.yaml, action: remove-dns}
stop: {action: restore-dns}
evidence: {baseline: [ok], inject: [ok], detect: [ok], mitigate: [ok], recover: [ok], prevent: [ok], cleanup: [ok]}
preventionTest: tests/render-contract.sh --case network-policy
cleanup:
  labels: {course.playbuilder.io/incident: INC-K8S-03}
  action: remove-fault
  externalSecretLifecycle: {targetSecretOwnerReferenceGC: true, providerSecretRetained: true}
