{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "sample-app.namespace" . }}
  annotations:
    argocd.argoproj.io/sync-options: Prune=false
  labels:
    {{- include "sample-app.labels" . | nindent 4 }}
    {{- toYaml .Values.namespace.labels | nindent 4 }}
{{- end }}
