id: INC-DB-04
chapter: 22
tier: Core-must
scope:
  environment: dev
  namespaces: [app-dev]
  forbiddenNamespaces: [app-prod, app-recovery, argocd, kube-system]
maximumDuration: 5m
inject:
  manifest: tests/fixtures/incident-index/static-structure.json
  action: reproduce-db-04
stop:
  action: stop-incident-writers
evidence:
  baseline: [baseline-observation]
  inject: [injection-accepted]
  detect: [detection-signal]
  mitigate: [mitigation-applied]
  recover: [recovery-confirmed]
  reconcile: [gitops-reconciled]
  prevent: [prevention-test]
  cleanup: [fault-resource-removed]
preventionTest: tests/render-contract.sh --case network-policy
cleanup:
  labels:
    course.playbuilder.io/incident: INC-DB-04
  action: remove-fault-resource
  externalSecretLifecycle:
    targetSecretOwnerReferenceGC: true
    providerSecretRetained: true
